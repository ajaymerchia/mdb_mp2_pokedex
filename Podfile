# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'mdb_mp2_pokedex' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mdb_mp2_pokedex

  target 'mdb_mp2_pokedexTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mdb_mp2_pokedexUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  
  pod 'ChameleonFramework/Swift', :git => 'https://github.com/ViccAlexander/Chameleon.git', :branch => 'wip/swift4', :inhibit_warnings => true

end
